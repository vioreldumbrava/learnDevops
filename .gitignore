# Secrets & local env
.env
*.pem
!devDockerKey.pem.example

# Backups & generated artifacts
/backups/
/artifacts/

# Go
/app/api/bin/
*.test
*.out

# Node / frontend
node_modules/
/app/frontend/dist/
/app/frontend/.vite/

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
*.pub
# Provider lock is platform-specific here (generated in CI/containers); let each
# machine regenerate it on `terraform init` to avoid cross-OS hash mismatches.
.terraform.lock.hcl

# Ansible
deploy/ansible/inventory.ini

# OS / editor
.DS_Store
Thumbs.db
.idea/
*.swp
