# Learning secret. In production, do NOT commit real secrets — use a sealed-secret /
# external-secrets operator or your cloud's secret manager, and rotate them.
apiVersion: v1
kind: Secret
metadata:
  name: dojo-secrets
  namespace: devops-dojo
type: Opaque
stringData:
  POSTGRES_PASSWORD: "change-me-in-real-life"
  DATABASE_URL: "postgres://dojo:change-me-in-real-life@db:5432/dojo?sslmode=disable"
